# Plotting

This directory contains scripts used to generate figures and visual summaries for the study.

The plotting scripts take processed result tables from the QTL, TWAS and GWAS integration analyses as input. Users should update the input and output paths in each script before running.

Example usage:

```bash
Rscript ./Plotting/example_plot.R
